#spring:
#  # H2 Database 설정
#  datasource:
#    driver-class-name: org.h2.Driver
#    url:  jdbc:h2:tcp://localhost/~/lustle  # H2 DB 연결 주소 (In-Memory Mode)
#    #url: 'jdbc:h2:~/test'    # H2 DB 연결 주소 (Embedded Mode)
#    username: sa       # H2 DB 접속 ID (사용자 지정)
#    password:       # H2 DB 접속 PW (사용자 지정)
spring:
  datasource: jdbc:mysql://34.66.120.50/Lustle?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  # H2 Console 설정
#  h2:
#    console: # H2 DB를 웹에서 관리할 수 있는 기능
#      enabled: true           # H2 Console 사용 여부
#      path: /h2-console       # H2 Console 접속 주소

#  # JPA 설정
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#    hibernate:
#      ddl-auto: create        # DB 초기화 전략 (none, create, create-drop, update, validate)
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.H2Dialect
#        format_sql: true      # 쿼리 로그 포맷 (정렬)
#        show_sql: true        # 쿼리 로그 출력